# The Zone

Plain React **JavaScript** app scaffolded with **Vite**.

## Getting started

```bash
npm install
npm run dev
```

Then open the local URL Vite prints (usually `http://localhost:5173`).

## Build / preview

```bash
npm run build
npm run preview
```



